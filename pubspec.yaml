name: flutter_application_1
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  filesystem_picker: ^3.1.0
  flutter:
    sdk: flutter
  flutter_p2p_connection: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Declare your asset path here
  assets:
    - assets/
    - assets/images/
